# Runtime env vars injected into the deployed container.
# On `hud deploy`, you'll be asked whether to sync these up (encrypted at rest on HUD).

# HUD API key — used inside the container for LLM judge grading via the HUD Gateway
HUD_API_KEY=your-key-here
